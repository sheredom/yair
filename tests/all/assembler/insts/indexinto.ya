// RUN: @yair_as @file | @yair_dis -

// CHECK: mod "😀"
mod "😀" {
  // CHECK: fn foo(ptr : *any, index : i32) : *any {
  fn foo(ptr : *any, index : i32) : *any {
    // CHECK: b0([[ptr:\w+]] : *any, [[index:\w+]] : i32):
    bar(ptr : *any, index : i32):
      // CHECK: [[r:\w+]] = indexinto i32, $$ptr, $$index
      r = indexinto i32, ptr, index
      // CHECK: ret $$r
      ret r
  }
}
