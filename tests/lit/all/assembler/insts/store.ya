// RUN: @yair_as @file | @yair_dis -

// CHECK: mod "😀"
mod "😀" {
  // CHECK: fn foo(a : *any, b : i8) : void {
  fn foo(a : *any, b : i8) : void {
    // CHECK: b0([[a:\w+]] : *any, [[b:\w+]] : i8):
    bar(a : *any, b: i8):
      // CHECK: store i8, $$a, $$b
      store i8, a, b
      // CHECK: ret
      ret
  }
}
